# ============================================
# API 配置
# ============================================
API_BASE_URL=http://localhost:3000
# API 密钥（必需！必须与后端配置的 API_KEY 完全一致）
# 重要: 必须手动填写，且必须与后端配置文件中的 API_KEY 完全相同！
# 未配置或密钥不匹配将导致所有数据上传失败！
API_KEY=

# ============================================
# 采集配置
# ============================================
POLL_INTERVAL=2000          # 轮询间隔（毫秒）
UPLOAD_INTERVAL=60000       # 上传间隔（毫秒，60秒）
MIN_USAGE_TIME=5000        # 最小使用时长（毫秒，小于5秒不记录）
HEARTBEAT_INTERVAL=30000   # 心跳间隔（毫秒，30秒）- 即使窗口未变化，也会定期上报状态保持在线

# ============================================
# 设备信息（可选）
# ============================================
# DEVICE_ID=PC-ComputerName
# DEVICE_NAME=My Workstation

